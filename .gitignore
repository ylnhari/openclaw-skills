# Per-install runtime state (NOT shipped with the skill, NOT committed)
# Created automatically by the skill on first successful run.
# Override location via MEDIUM_BLOG_CONFIG env var.
config.local.json

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Node (used by examples and tooling)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment files (NEVER commit secrets)
.env
.env.*
!.env.example

# Build output
dist/
build/
out/
*.tgz

# Local skill drafts (uncommitted work-in-progress)
drafts/
scratch/
